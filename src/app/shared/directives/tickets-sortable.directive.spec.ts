import { TicketsSortableDirective } from './tickets-sortable.directive';

describe('TicketsSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketsSortableDirective();
    expect(directive).toBeTruthy();
  });
});
