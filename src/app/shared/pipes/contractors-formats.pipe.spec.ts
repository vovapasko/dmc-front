import { ContractorsFormatsPipe } from './contractors-formats.pipe';

describe('ContractorsFormatsPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractorsFormatsPipe();
    expect(pipe).toBeTruthy();
  });
});
