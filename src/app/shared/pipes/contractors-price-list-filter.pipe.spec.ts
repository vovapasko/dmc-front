import { ContractorsPriceListFilterPipe } from './contractors-price-list-filter.pipe';

describe('ContractorsPriceListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractorsPriceListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
