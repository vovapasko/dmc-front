import { ContractorsNewsAmountPipe } from './contractors-news-amount.pipe';

describe('ContractorsNewsAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractorsNewsAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
