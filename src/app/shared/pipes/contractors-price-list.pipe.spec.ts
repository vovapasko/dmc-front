import { ContractorsPriceListPipe } from './contractors-price-list.pipe';

describe('ContractorsPriceListPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractorsPriceListPipe();
    expect(pipe).toBeTruthy();
  });
});
