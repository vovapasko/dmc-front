export interface Buttons {
    color: string;
    name: string;
}

export interface Icons {
    name: string;
    color: string;
}

export interface IconButton {
    name: string;
    color: string;
    icon: string;
}
